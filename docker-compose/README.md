# docker-compose

This repository will be used to deploy sample python application with redis in docker containers. It will start redis and python in separate containers.

### Installation
cd /path/to/folder

docker-compose up
